import Token from './Tokens/Token';
import TreeNode from './TreeNode';
class Parser {

    buildParseTree(tokens: Token[]) : TreeNode {
        let root = new TreeNode();

        return root;
    }

    toReversePolishNotation(tokens: Token[]) {
        var rpnArray: Token[] = [];
        
        return rpnArray;
    }
}